# Obtain from @BotFather. Do not commit a filled .env file.
BOT_TOKEN=replace_me
# Comma-separated Telegram numeric IDs allowed to use the bot.
ALLOWED_USER_IDS=123456789
DATABASE_PATH=data/expenses.sqlite3
DEFAULT_TIMEZONE=Asia/Yekaterinburg
# Generate securely: python -c "import base64,secrets; print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())"
ENCRYPTION_KEY=replace_with_a_32_byte_fernet_key
